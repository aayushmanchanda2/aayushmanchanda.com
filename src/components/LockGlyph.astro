---
/** A padlock, 12px, in the row's ink: a private row in the pane (VET-274). The markup is `lib/pane-merge.ts › LOCK_SVG`, which public pages draw too. */
import { LOCK_SVG } from "../lib/pane-merge";
---

<Fragment set:html={LOCK_SVG} />
